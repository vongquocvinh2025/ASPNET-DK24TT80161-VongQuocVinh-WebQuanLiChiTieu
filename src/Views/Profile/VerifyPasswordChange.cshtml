@{
    ViewData["Title"] = "Xác nhận đổi mật khẩu";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<div class="row justify-content-center">
    <div class="col-lg-6">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title mb-0">Xác nhận OTP</h4>
            </div>
            <div class="card-body">
                <p>Một mã OTP đã được gửi đến email <strong>@User.Identity.Name</strong>. Vui lòng kiểm tra và nhập mã vào ô bên dưới để hoàn tất việc đổi mật khẩu.</p>
                <form asp-action="VerifyPasswordChange" method="post">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="mb-3">
                        <label for="otp" class="form-label">Mã OTP</label>
                        <input type="text" name="otp" class="form-control" required />
                    </div>
                    <button type="submit" class="btn btn-primary">Xác nhận</button>
                </form>
            </div>
        </div>
    </div>
</div>